<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yourorg.sample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogInActivity"></activity>
        <activity android:name=".DataVisualizationActivity" />
        <activity android:name=".TrackPurchasesActivity" />
        <activity android:name=".AddUserActivity" />
        <activity android:name=".ShopActivity" />
        <activity android:name=".PriceBookActivity" />
        <activity android:name=".ViewItemsActivity" />
        <activity android:name=".ViewListsActivity" />
        <activity android:name=".ViewRooms1Activity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".ui.login.LogInActivity"
            android:label="@string/title_activity_log_in" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>